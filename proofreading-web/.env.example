# ===========================================
# PDF Proofreading - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values

# Backend API URL
# For local development: http://localhost:8000
# For production: Your Cloud Run URL (e.g., https://proofslab-api-xxxxx-ew.a.run.app)
NEXT_PUBLIC_API_URL=http://localhost:8000

# ===========================================
# Firebase Authentication (Required)
# ===========================================
# Get these from Firebase Console > Project Settings > General > Your apps
NEXT_PUBLIC_FIREBASE_API_KEY=your-api-key
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=your-project.firebaseapp.com
NEXT_PUBLIC_FIREBASE_PROJECT_ID=your-project-id

# ===========================================
# Stripe Payment (Required for monetization)
# ===========================================
# Get these from Stripe Dashboard > Developers > API Keys
# Use test keys for development (pk_test_..., sk_test_...)
# Use live keys for production (pk_live_..., sk_live_...)

# Frontend - Publishable key (safe to expose)
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=pk_test_...

# Backend secrets (stored in GCP Secret Manager for Cloud Run)
# These are NOT needed in .env.local - only documented here for reference
# - STRIPE_SECRET_KEY: API secret key from Stripe Dashboard
# - STRIPE_WEBHOOK_SECRET: Webhook signing secret from Stripe Dashboard > Webhooks
# - STRIPE_PRICE_ID_MONTHLY: Price ID for monthly Pro plan (price_...)
# - STRIPE_PRICE_ID_YEARLY: Price ID for yearly Pro plan (price_...)
